spring:
  application:
    name: bsp-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ip:8848
      config:
        server-addr: ip:8848
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true